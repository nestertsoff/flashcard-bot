services:
  app:
    image: ${IMAGE}
    ports:
      - "3000:3000"
    volumes:
      - app-data:/app/data
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  app-data:
