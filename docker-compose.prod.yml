services:
  app:
    image: ${IMAGE}
    ports:
      - "80:3000"
    volumes:
      - app-data:/app/data
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - GOOGLE_TTS_KEY=${GOOGLE_TTS_KEY}
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  app-data:
