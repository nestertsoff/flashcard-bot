services:
  bot:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - bot-data:/app/data
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - WEBAPP_URL=${WEBAPP_URL}
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  bot-data:
